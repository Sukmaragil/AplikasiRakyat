<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:background="@drawable/list_row_selector"
    android:padding="8dp" >

    <!-- Thumbnail Image -->
    <com.android.volley.toolbox.NetworkImageView
        android:id="@+id/thumbnail"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="8dp" />

    <!--Title -->
    <TextView
        android:id="@+id/namaDepan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/thumbnail"
        android:layout_toEndOf="@+id/thumbnail"
        android:textSize="@dimen/namaDepan"
        android:textStyle="bold" />

    <!-- Rating -->
    <TextView
        android:id="@+id/nik"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/namaDepan"
        android:layout_marginTop="1dip"
        android:layout_toEndOf="@+id/thumbnail"
        android:textSize="@dimen/nik" />

    <!-- Genre -->
    <TextView
        android:id="@+id/jenisPelayanan"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/nik"
        android:layout_marginTop="5dp"
        android:layout_toEndOf="@+id/thumbnail"
        android:textColor="@color/jenisPelayanan"
        android:textSize="@dimen/jenisPelayanan" />

    <!-- Release Year -->

</RelativeLayout>